<?php
/**
 * ОБРАЗЕЦ конфигурации.
 *
 * НА СЕРВЕРЕ этот файл должен лежать НЕ здесь, а на уровень выше public_html:
 *     /home/<буква>/<логин>/moremontessori.ru/config.php
 * Тогда он недоступен из браузера и не попадает в git.
 *
 * Скопируйте содержимое, подставьте свои значения и сохраните как config.php
 * в директории сайта РЯДОМ с public_html.
 */

return [
    // Токен нового бота от @BotFather. Старый токен обязательно отозвать.
    'telegram_token'     => 'СЮДА_НОВЫЙ_ТОКЕН',

    // ID чата/группы, куда падают заявки
    'telegram_chat_id'   => '-1003500738124',

    // ID топика внутри группы; если топиков нет — поставьте null
    'telegram_thread_id' => 2,
];
